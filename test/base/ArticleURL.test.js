import ArticleURL from '../out/app/models/ArticleURL';

describe('ArticleURL class', function() {
	it('should reject empty parameters', function() {
		new ArticleURL();
	});

	it('should parse an URL correctly', function() {
		// examples.forEach(testParser);
	});
})

apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{env "REPONAME"}}
  namespace: {{env "REPONAME"}}
spec:
  selector:
    matchLabels:
      app: {{env "REPONAME"}}
  replicas: 2
  revisionHistoryLimit: 1
  template:
    metadata:
      labels:
        app: {{env "REPONAME"}}
    spec:
      containers:
        - name: {{env "REPONAME"}}
          image: {{env "IMAGE"}}
          command: ["npm"]
          args: ["start"]
          ports:
          - containerPort: 4000
            name: http-port
          envFrom:
            - secretRef:
                name: secrets
            - configMapRef:
                name: config
          livenessProbe:
            httpGet:
              path: /api/version
              port: http-port
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/version
              port: http-port
            timeoutSeconds: 5

